[theme]
primaryColor             = "#F2A900"   # GIX 黄金色 — 按钮、交互高亮
backgroundColor          = "#FFFFFF"   # 主内容区背景 — 白
secondaryBackgroundColor = "#EDE7F6"   # 次级背景 — 浅紫（侧边栏由 CSS 单独覆盖为深紫）
textColor                = "#1A1A1A"   # 主区域文字 — 深色
font                     = "sans serif"
